services:
  - type: web
    name: flowchart-tasks-app
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: RENDER_EXTERNAL_URL
        sync: false
    healthCheckPath: /ping
